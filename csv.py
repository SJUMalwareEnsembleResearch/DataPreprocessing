import csv

fields = ["id", "family", "amount"]
for i in range 1000:
	string = "op" + str(i)
	fields.append(string)

filename = "opcodes.csv"

dataset = []
for ()

with open (filename, 'rw') as csvfile:

